const friends = ['marcos', 'flavia', 'pedro', 'kayky']//arrays de string
const num = [7, 8, 9]// array de number

num.push(10, 11, 12)//envia no final

num.unshift(1, 2, 3)// no começo

num.splice(3, 0, 4, 5, 6)//regra para poder começar de um ponto de partida e apagar algum se precisar

console.log(friends)//saida de dados
console.log(num)//saida de dados 